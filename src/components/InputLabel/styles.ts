import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: "flex-start"
  },

  title: {
    marginTop: 20,
    fontSize: 20,
    fontWeight: 'bold',
  },

  input: {
    marginTop: 9,
    backgroundColor: '#E5B15B',
    padding: 8,
    borderRadius: 8,
    width: 310,
    height: 50,
    color: "#003459",
  }
  
});

export default styles;